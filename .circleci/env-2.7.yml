name: pyreshaper-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - bumpversion
  - codecov
  - coverage
  - flake8
  - isort
  - mpich
  - mpi4py
  - netcdf4
  - pynio
  - pytest
  - pytest-cov
  - python=2.7
  - pip
  - pip:
    - asaptools
